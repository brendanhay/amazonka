instance ToHeaders {{ scope.name }} where
{% if scope.headers | listLength > 0 %}
    toHeaders {{ scope.name }}{..} =
  {% for hdr in scope.headers %}
    {% if hdr.first %}
        [ "{{ hdr.value.location_name }}" =: {{ hdr.value.prefixed }}
    {% else %}
        , "{{ hdr.value.location_name }}" =: {{ hdr.value.prefixed }}
    {% endif %}
  {% endfor %}
        ]
{% else %}
    toHeaders = const mempty
{% endif %}
